package com.orderofdev.tasker.view.details

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class TaskDetailsViewModel (private val app: Application) : AndroidViewModel(app) {
}